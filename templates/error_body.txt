{# エラー通知の本文テンプレート。使える変数: area_name / watch_id / consecutive_failures / error_message / now #}
{{ area_name }} の空室情報を {{ consecutive_failures }} 回連続で取得できていません。

楽天トラベル側の一時的な不調か、ページ構造の変更が考えられます。
数時間たっても復旧しない場合は、GitHub Actions の実行ログを確認してください。

■ 直近のエラー内容
{{ error_message }}

------------------------------------------------------------
本メールは自動送信です。このメールは復旧するまで1日1回だけ送られます。
検知日時: {{ now.strftime("%Y-%m-%d %H:%M") }}（日本時間）
